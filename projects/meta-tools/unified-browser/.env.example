# Unified Browser MCP Server Configuration

# Default provider when auto-routing isn't possible
# Options: playwright, puppeteer
DEFAULT_BROWSER_PROVIDER=playwright

# MCP Docker gateway URL (if using remote providers)
# Leave blank if providers are called through MCP gateway
MCP_DOCKER_URL=

# Session management
MAX_SESSIONS=5
SESSION_TIMEOUT_MS=600000
